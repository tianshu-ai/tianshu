# 0.5.0 文档更新计划

上一个「圆整」tag 是 **v0.4.0（2026-06-28）**。之后整条 0.4.x 线（~60 个补丁版，到 0.4.85）
积累了几块大功能，够分量升 **0.5.0**。已有 release-please 的 0.5.0 PR #259，但它是 06-29 开的、
早于这些功能落地 + 早于 hotfix 合并回 main，**已过时，changelog 需重生成**。

## 本次要高亮的新功能（按分量排序）

| # | 功能 | 提交量 | 一句话 |
|---|---|---|---|
| 1 | **Workforce Studio（Solution IDE）** | 20 | 把「主 agent + 各 worker + 插件集」抽象成 Solution，可提取/编辑/diff/激活；三栏 IDE；导出/导入为文件；per-worker 覆盖 |
| 2 | **OpenCode worker** | 10 | 全新 worker 类型：opencode + oh-my-openagent 跑在预建沙箱镜像里，经模型代理接单一模型；近实时 transcript + 工具 chips |
| 3 | **Network Policy 管理页（openshell）** | 4 | 沙箱 egress 拦截可视化 + allow-list + 逐条 Allow 按钮 |
| 4 | **模型调用韧性** | 4 | 限流感知重试（按 Retry-After 退避）+ 客户端断线自动指数退避重连（到 30min）+ 恢复中断的 turn |
| 5 | **Models 管理页（Settings）** | 3 | 图形化维护 provider 目录（读写 config.json），apiKey 掩码，默认模型下拉 |
| 6 | **Web search** | 2 | 免 key 的 Exa/Parallel 后端 + `web_fetch` 工具 + 可选搜索方案 |
| 7 | **Workboard** | 3 | 一次 batch 定义任务依赖图 + Execution 对话框「Raw log」视图 |
| 8 | 剪贴板粘贴附件、CLI update 后跑 doctor 等零碎改进 | — | |

## 要改的文档（及具体动作）

### 1. README.md（最重要，对外门面）
- **`✨ What you get`（L140）**：现在只讲 browser/sandbox/workspace + workers。**补一段** Workforce Studio（"不止调度 worker，还能把整套 agent 配置抽象成可版本化、可 diff、可一键激活的 Solution"）和 OpenCode worker。这是 0.4→0.5 最大的叙事变化。
- **`🗺️ Roadmap`（L352）**：现在写着 "Shipped (0.3.x)" + "Next (0.4.x)"。改成：
  - 新增 **Shipped (0.4.x → 0.5.0)** 段，勾掉已完成的（Workforce Studio、OpenCode worker、Network Policy UI、Models UI、web search、model retry/resilience）。
  - "Next" 段更新为 0.5.x/0.6 真实待办（Docker 镜像、systemd、skills marketplace、orchestrator analytics、**以及 review 里的 auth/授权登录**——B1/B2 就归到这里）。
- **顶部徽章/一句话**：无需改（badge 自动读 npm/release）。可考虑 hero 截图换成带 Workforce Studio 或 Models 页的新界面。
- README.zh-CN.md 同步。

### 2. docs/
- **新增 `docs/workforce-studio.md`**（或指向已存在的 `docs/architecture/solutions.md`）——面向用户的「怎么用 Solution IDE」而非架构文。
- **新增/更新 opencode worker 说明**（`docs/architecture/workers.md` 已有架构，缺用户向的「如何启用 opencode worker」）。
- **`docs/config.md` / `getting-started.md` / `known-models.md`**：现在教手改 `config.json` 配 model。**加一句**：现在也可以在 Settings → Models 图形化维护（指向新 UI），手改仍支持、两者同步。
- **`docs/getting-started.md`**：可加 Network Policy 页、Models 页的截图/入口说明。

### 3. CHANGELOG.md
- release-please 会在 0.5.0 PR 里自动按 conventional commits 生成。**建议**：让 release-please 重新生成（#259 需要 rebase 或关掉重开），别手写。我们只需保证 commit message 规范（已规范）。
- 顶部可手工加一个「0.5.0 Highlights」小结段，把上面表格前 5 项用人话写出来（release-please 生成的是按 scope 分类的流水，不够 highlight）。

## 发布流程建议（0.5.0）
1. 先把当前 `hotfix/0.3.2`（含 0.4.81-0.4.85 的 Models/retry/review 工作）合回 main（上次那个 PR 模式）。
2. main 上让 release-please 重新算 0.5.0（#259 rebase 或重开），它会生成完整 changelog + 开 release PR。
3. review 里的 **B1（admin 授权）/B2（apiKey ${VAR} 外泄）** 是 0.5.0 前**应该权衡**的：要么在 0.5.0 前修（因为 Models/MCP admin 页是 0.5.0 高亮功能，暴露面扩大了），要么在 README/release note 里**明确标注**"admin 页目前无鉴权，仅限单用户/可信部署，多用户鉴权在下个版本"。**建议至少加这条安全说明**，别让用户误以为 admin 页可以在多租户下安全暴露。
4. 合并 release PR → 自动 tag v0.5.0 + 发 npm。

## ⚠️ 需要你拍板的点
- (a) 0.5.0 前是否补一条 **admin 页安全免责声明**（强烈建议，成本低）？还是先修 B1 最小 gate？
- (b) 文档我可以直接改 README/docs（但按工作区规则，对外发布内容需你确认）——你要我**先出 README diff 给你审**，还是直接改完你 review？
- (c) hero 截图要不要换新（我可以截 Workforce Studio / Models 页）？
